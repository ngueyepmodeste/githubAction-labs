name: CI

on: [push]

jobs:
  test2:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
       - name: installer les paquets systems      
         run:  apt-get update -y 

       - name: mise à jours du system 
         run: apt-get upgrade -y 
       - name: installer nginx && docker 
         run: apt-get install nginx -y && apt-get install docker.io -y
       - name: test 
         run: docker --version && systemctl nginx status 
